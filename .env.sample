# ---- Django ----
SECRET_KEY=change-me-for-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# ---- DB (choose one) ----
# SQLite (dev default; keep settings if you use sqlite)
# For MySQL:
DB_NAME=cs619
DB_USER=root
DB_PASSWORD=your_password
DB_HOST=127.0.0.1
DB_PORT=3306

# ---- CORS ----
CORS_ALLOW_ALL_ORIGINS=True

# ---- JWT ----
ACCESS_MINUTES=60
REFRESH_DAYS=7

# ---- App meta ----
APP_NAME="CS619 Construction Materials"
APP_VERSION=1.0.0
